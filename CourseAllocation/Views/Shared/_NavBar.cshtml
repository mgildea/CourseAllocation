@{ 
    string controller = ViewContext.Controller.ValueProvider.GetValue("controller").RawValue.ToString();
    string action = ViewContext.Controller.ValueProvider.GetValue("action").RawValue.ToString();
}

<div class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            @Html.ActionLink("Course Allocation", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
        </div>
        <div class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
 
                
                @if (controller == "Home" && action == "Index")
                {
                    <li class="ignore"><a href="#no-spy-first"></a></li>
                    <li><a href="#Team" class="spythis">Team</a></li>
                    <li><a href="#ReadMe" class="spythis">Read Me</a></li>
                }
                else
                {
                    <li><a href="@Url.Action("Index", "Home")#Team" class="spythis">Team</a></li>
                    <li><a href="@Url.Action("Index", "Home")#ReadMe" class="spythis">Read Me</a></li>
                }



                @if(User.Identity.IsAuthenticated)
                {
                    <li>@Html.ActionLink("Admin", "Index", "Admin")</li>  
                }
              @* <li>@Html.ActionLink("About", "About", "Home")</li>


    <li><a href="#"><i class="fa fa-facebook"></i></a></li>
    <li><a href="#"><i class="fa fa-twitter"></i></a></li>*@ 
            </ul>

           
            @Html.Partial("_LoginPartial")
             
            @Html.Partial("_NotificationsPartial")
</div>
    </div>
</div>
